syntax = "proto3";

import "map.proto";
option go_package = "factorio/grpc";

package grpc;

service Mine {
  rpc sendResource (Item) returns (Empty) {}
  rpc giveResource (Item) returns (Item) {}
  rpc neededResource (Empty) returns (Item) {}
}
